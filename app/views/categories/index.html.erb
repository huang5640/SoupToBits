<h1>Categories</h1>
<ul>
  <% @categories.each do |category|%>
  <li><%= link_to category.name, category%>(<%= category.soups.count%>)</li>
  <% end %>
    <% if session[:vip] %>
	<li>Super secret Soup</li>
  <% end %>
</ul>